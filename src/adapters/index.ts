/**
 * Schema Adapters
 * Export all available schema adapters
 */

export { MCPAdapter } from './mcp.js';

// Export types for convenience
export type { SchemaAdapter, SchemaRef } from '../core/types.js';
